export default function Slider(cardArr) {
    let sliderContainer = document.createElement('div');
    sliderContainer.innerHTML = `
    ${cardArr.map((card)=> {
        
    })}
    `
}

function Card() {
    let CardContainer = document.createElement('div');
    CardContainer.innerHTML = `

    `
}